import { Settings } from "../types";
export declare function splitIntoMultiSeries(settings: Settings, data: any, { stack, excludeEmpty }?: {
    stack?: boolean;
    excludeEmpty?: boolean;
}): any[];
