import * as d3 from "d3";
import { Settings } from "../types";
export type TreeData = {
    name: string;
    children: unknown;
    size?: number;
    color?: number;
    tooltip?: unknown[];
};
export declare function treeData(settings: Settings): {
    split: string;
    data: d3.HierarchyNode<TreeData>;
    extents: number[];
}[];
export declare const getDataValue: (d: any, aggregate: any, split: any) => any;
