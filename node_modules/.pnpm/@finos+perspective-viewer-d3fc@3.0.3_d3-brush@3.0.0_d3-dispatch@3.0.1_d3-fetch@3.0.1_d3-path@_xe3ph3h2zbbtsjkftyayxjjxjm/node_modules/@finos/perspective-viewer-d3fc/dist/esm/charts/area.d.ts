import { HTMLSelection, Settings } from "../types";
declare function areaChart(container: HTMLSelection, settings: Settings): void;
declare namespace areaChart {
    var plugin: {
        name: string;
        category: string;
        max_cells: number;
        max_columns: number;
        render_warning: boolean;
        initial: {
            names: string[];
        };
    };
}
export default areaChart;
