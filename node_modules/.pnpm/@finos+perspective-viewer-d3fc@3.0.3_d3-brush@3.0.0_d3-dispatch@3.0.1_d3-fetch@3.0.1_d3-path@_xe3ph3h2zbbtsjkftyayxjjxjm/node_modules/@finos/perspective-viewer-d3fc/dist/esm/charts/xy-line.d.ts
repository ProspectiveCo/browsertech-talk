import { HTMLSelection, Settings } from "../types";
declare function xyLine(container: HTMLSelection, settings: Settings): void;
declare namespace xyLine {
    var plugin: {
        name: string;
        category: string;
        max_cells: number;
        max_columns: number;
        render_warning: boolean;
        initial: {
            type: string;
            count: number;
            names: string[];
        };
        selectMode: string;
    };
}
export default xyLine;
