import { HTMLSelection, Settings } from "../types";
declare function sunburst(container: HTMLSelection, settings: Settings): void;
declare namespace sunburst {
    var plugin: {
        name: string;
        category: string;
        max_cells: number;
        max_columns: number;
        render_warning: boolean;
        initial: {
            type: string;
            count: number;
            names: string[];
        };
    };
}
export default sunburst;
