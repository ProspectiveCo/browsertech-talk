import { HTMLSelection, Settings } from "../types";
declare function yScatter(container: HTMLSelection, settings: Settings): void;
declare namespace yScatter {
    var plugin: {
        name: string;
        category: string;
        max_cells: number;
        max_columns: number;
        render_warning: boolean;
        initial: {
            names: string[];
        };
    };
}
export default yScatter;
