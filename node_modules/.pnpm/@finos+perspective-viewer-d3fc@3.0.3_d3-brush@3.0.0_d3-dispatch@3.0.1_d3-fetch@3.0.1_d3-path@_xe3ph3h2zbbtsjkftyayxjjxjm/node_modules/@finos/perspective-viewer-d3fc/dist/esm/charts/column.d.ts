import { HTMLSelection, Settings } from "../types";
declare function columnChart(container: HTMLSelection, settings: Settings): void;
declare namespace columnChart {
    var plugin: {
        name: string;
        category: string;
        max_cells: number;
        max_columns: number;
        render_warning: boolean;
        initial: {
            names: string[];
        };
    };
}
export default columnChart;
