import { HTMLSelection, Settings } from "../types";
declare function treemap(container: HTMLSelection, settings: Settings): void;
declare namespace treemap {
    var plugin: {
        type: string;
        name: string;
        category: string;
        max_cells: number;
        max_columns: number;
        render_warning: boolean;
        initial: {
            type: string;
            count: number;
            names: string[];
        };
    };
}
export default treemap;
