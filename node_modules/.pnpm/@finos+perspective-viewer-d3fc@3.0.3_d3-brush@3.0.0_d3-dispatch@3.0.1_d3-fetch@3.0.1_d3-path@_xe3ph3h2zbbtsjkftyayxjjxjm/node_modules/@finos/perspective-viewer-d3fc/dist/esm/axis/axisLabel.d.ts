import { Settings } from "../types";
export interface LabelFunction {
    (d: any, i?: any): string;
    valueName(): string;
    valueName(nextValueName: string): this;
}
export declare const labelFunction: (settings: Settings) => LabelFunction;
